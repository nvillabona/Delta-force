import { FilterConsultorPipe } from './filter-consultor.pipe';

describe('FilterConsultorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterConsultorPipe();
    expect(pipe).toBeTruthy();
  });
});
