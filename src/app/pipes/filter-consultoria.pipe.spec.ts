import { FilterConsultoriaPipe } from './filter-consultoria.pipe';

describe('FilterConsultoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterConsultoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
