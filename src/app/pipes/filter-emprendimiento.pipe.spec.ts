import { FilterEmprendimientoPipe } from './filter-emprendimiento.pipe';

describe('FilterEmprendimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEmprendimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
